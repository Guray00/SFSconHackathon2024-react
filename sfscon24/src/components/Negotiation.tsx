import { AccordionItem } from "@nextui-org/react";


export default function Negotiation({src, dst, min, max, status}: {src: string, dst: string, min: number, max: number, status: string}) {
    
    return (
        <AccordionItem aria-label="Accordion 1" title="Accordion 1">
        {"ciao"}
        </AccordionItem>
    );

}